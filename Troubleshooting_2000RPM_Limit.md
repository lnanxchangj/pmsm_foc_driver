# 排错分析：速度环设定大于2000 RPM时测速停留在2000 RPM的原因

根据对MCSDK 6.4.2工程源码（`pmsm_foc_driver`）以及参数配置的深入分析，软件限幅和控制逻辑并没有问题，问题出在**编码器定时器（TIM3）的硬件输入捕获滤波器（Input Capture Filter）设置过高，导致高频正交脉冲被当成噪声滤除**。

以下是详细的原因分析及数学推导：

### 1. 软件限幅与溢出排查（排除软件嫌疑）
在代码中，您的最高应用速度（`MAX_APPLICATION_SPEED_RPM`）确实被正确地配置为了 `5000`。
上位机下发的目标速度会转换成 `SPEED_UNIT`（0.1Hz），5000 RPM 对应 `833`，这个数值在 MCSDK 的 `int16_t` 和 `int32_t` 的运算过程中（如 `MCI_ExecSpeedRamp` 和 `STC_ExecRamp`）**均未发生任何数据类型溢出**，PI速度环的积分输出限幅（`IQMAX`）也是正常的。软件是完全允许电机跑到 5000 RPM 的。

### 2. 核心原因：编码器线数过高 + 滤波太强
您的电机编码器线数（`M1_ENCODER_PPR`）高达 **2000线**。这意味着在正交模式下，电机每转一圈会产生 8000 个边沿。

我们在 `Inc/parameters_conversion.h` 和 `pmsm_foc_driver.ioc` 中看到，TIM3 的输入捕获滤波器被设置为了 `12`：
```c
#define M1_ENC_IC_FILTER_LL LL_TIM_IC_FILTER_FDIV16_N8
#define M1_ENC_IC_FILTER    12
```

**我们来算一笔账：**
* **滤波器限制时间：** 在 STM32F407 中，TIM3 挂载在 APB1 上（时钟为 84MHz）。滤波参数 `FDIV16_N8` 意味着采样频率为 $84\text{MHz} / 16 = 5.25\text{MHz}$，并且必须连续采集到 8 个相同的高/低电平才认为边沿有效。
因此，硬件能识别的**最短有效脉冲宽度**为：$8 \times (1 / 5.25\text{MHz}) \approx \mathbf{1.52 \mu s}$。任何小于 $1.52 \mu s$ 的脉冲都会被硬件直接扔掉。

* **2000 RPM时的脉冲间隔：** 当电机转速达到 2000 RPM 时，AB相边沿触发的理论最小间隔为：
  $T = 60 \text{秒} / (2000 \text{转} \times 8000 \text{次边沿}) = \mathbf{3.75 \mu s}$。

**为什么正好卡在2000？**
理论上 $3.75 \mu s > 1.52 \mu s$，似乎可以通过。但现实中的光电/磁编码器普遍存在**占空比不对称**和**正交相位偏差**（不是完美的90度），加上电机运行的机械抖动，实际的边沿间隔很容易缩小到理论值的一半甚至更低。
当速度达到并超过 2000 RPM 时，AB相之间较窄的那个时间差就会开始小于 $1.52 \mu s$，**导致 TIM3 硬件滤波器把正常的编码器脉冲当做“毛刺”给吃掉了**。

### 3. 闭环系统产生的现象解释
由于定时器漏掉了脉冲，MCSDK 在 `ENC_CalcAvrgMecSpeedUnit` 函数中算出的“测量转速”就会下降或饱和。
此时闭环系统的表现就是：
1. 上位机下发 5000 RPM 目标值。
2. PI速度环发现当前测得的速度“只有2000 RPM”，于是疯狂增大 Iq（电流/转矩）指令到最大值。
3. 电机在物理上开始狂转（甚至可能已经达到了极高的转速或者撞到了反电动势的电压极限）。
4. 但由于硬件漏读脉冲，软件算出来的转速**依然只显示为 2000 RPM**。
5. 最终在上位机图表上，您看到的现象就是“只要设定目标大于2000，转速依然只会停留在2000”。

### 解决办法
您需要降低编码器输入捕获的滤波强度，释放高频脉冲的通行权。

**通过 ST Motor Control Workbench 修改：**
1. 打开您的 `.stwb6` 工程 (`pmsm_foc_driver.stwb6`)。
2. 进入 `Speed/Position feedback`（速度/位置反馈）配置模块。
3. 找到 **Encoder Filter**（编码器滤波器）设置。
4. 将它的值从 12 降低到 **`0`、`1` 或 `2`**（对于2000线的高分辨率编码器，强烈建议设为 0 或 1 即可）。
5. 点击 Generate Code 重新生成代码，再编译烧录即可正常突破 2000 RPM。

*(如果您习惯直接改代码，可以在 `pmsm_foc_driver.ioc` 和 CubeMX 中将 `TIM3.IC1Filter` 和 `IC2Filter` 的值改为 0~2 后重新生成，或者在 Workbench 中进行上述修改。)*
