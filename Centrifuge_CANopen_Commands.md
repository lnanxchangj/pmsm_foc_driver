# 离心机 CANopen 通讯指令整理

本项目通过 CAN 总线（NodeID = 1）控制离心机电机，遵循 CANopen DS301 通讯协议和 DS402 运动控制协议。

## 1. 通讯基础配置 (NodeID: 1)

| 通讯类型 | CAN ID (COB-ID) | 说明 |
| :--- | :--- | :--- |
| **NMT** | `0x000` | 网络管理指令 |
| **SDO (TX)** | `0x601` | 发送指令 (Client -> Server) |
| **SDO (RX)** | `0x581` | 接收反馈 (Server -> Client) |
| **RPDO1** | `0x201` | 快速控制指令 (映射至 6040h 控制字) |

---

## 2. NMT 网络管理指令

用于切换驱动器的工作状态（初始化、运行、停止）。

| 指令名称 | 帧内容 (Hex) | 发送 ID | 功能说明 |
| :--- | :--- | :--- | :--- |
| **Start Node** | `01 01` | `0x000` | 启动节点 1，进入 Operational 状态 |

---

## 3. DS402 状态机控制 (Controlword: 6040h)

通过修改控制字切换驱动器状态机。代码中通过 PDO (`0x201`) 或 SDO (`0x601`) 发送。

| 状态指令 | 帧内容 (Hex) | 发送 ID | 说明 |
| :--- | :--- | :--- | :--- |
| **Ready to Switch On** | `06 00` | `0x201` | 准备就绪 |
| **Switch On** | `07 00` | `0x201` | 切换至开启 |
| **Operation Enable** | `0F 00` | `0x201` | 使能运行 |
| **Fault Reset** | `80 00` | `0x201` | 故障复位 |

---

## 4. 运动模式配置 (SDO: 0x601)

### 4.1 模式切换 (Object 6060h)
| 模式名称 | 帧内容 (Hex) | 说明 |
| :--- | :--- | :--- |
| **Position Mode** | `2F 60 60 00 01 00 00 00` | 切换至位置模式 (Profile Position) |
| **Velocity Mode** | `2F 60 60 00 03 00 00 00` | 切换至速度模式 (Profile Velocity) |
| **Homing Mode** | `2F 60 60 00 06 00 00 00` | 切换至回零模式 (Homing) |

### 4.2 运动参数设置
| 参数类型 | 对象索引 | 帧内容示例 (Hex) | 说明 |
| :--- | :--- | :--- | :--- |
| **目标位置** | `607Ah` | `23 7A 60 00 [DD DD DD DD]` | 设置目标脉冲位置 (4字节) |
| **目标速度** | `60FFh` | `23 FF 60 00 [DD DD DD DD]` | 设置运行速度 (4字节) |
| **运动加速度** | `6083h` | `23 83 60 00 20 00 00 00` | 设置加速度 |
| **位置模式速度** | `6081h` | `23 81 60 00 1C AA 00 00` | 设置位置模式下的限制速度 |

---

## 5. 回零 (Homing) 相关指令

| 功能名称 | 帧内容 (Hex) | 发送 ID | 说明 |
| :--- | :--- | :--- | :--- |
| **设置回零速度** | `23 99 60 02 00 00 01 00` | `0x601` | 索引 6099h sub 02 |
| **设置回零加速度** | `23 9A 60 00 20 00 00 00` | `0x601` | 索引 609Ah |
| **设置回零偏移** | `23 7C 60 00 00 00 00 00` | `0x601` | 索引 607Ch |
| **选择回零方式** | `2F 98 60 00 21 00 00 00` | `0x601` | 索引 6098h (0x21=33: 寻零位点) |
| **启动回零动作** | `1F 00` | `0x201` | 控制字 6040h 置为 0x001F |

---

## 6. 数据读取与反馈 (SDO: 0x601)

| 读取内容 | 帧内容 (Hex) | 反馈 ID | 说明 |
| :--- | :--- | :--- | :--- |
| **读取当前位置** | `40 64 60 00 00 00 00 00` | `0x581` | 对应索引 6064h |
| **读取实际速度** | `40 6C 60 00 00 00 00 00` | `0x581` | 对应索引 606Ch |
| **查询错误状态** | `40 00 20 00 00 00 00 00` | `0x581` | 对应索引 2000h (自定义或特定厂商) |

---

## 7. 注意事项
1. **字节序**：CANopen 使用 **小端模式 (Little-Endian)**。例如目标位置 `0x00001F40` 发送时应为 `40 1F 00 00`。
2. **SDO 命令字**：
   - `0x23`: 发送 4 字节数据
   - `0x2B`: 发送 2 字节数据
   - `0x2F`: 发送 1 字节数据
   - `0x40`: 读取请求
   - `0x60`: 写入成功反馈
3. **PDO 发送**：代码中 `CAN1_Send_Msg3` 专门用于发送 ID 为 `0x201` 的 RPDO1，其长度通常根据驱动器映射配置（代码中多处使用了 2 字节或 8 字节发送）。
